'use strict';

const defaultPic = document.getElementsByClassName('avatar');

let changePic = () => {
    let theInput = document.createElement("INPUT");
    theInput.setAttribute("type", "file");
    let children = document.body.appendChild(theInput);


}